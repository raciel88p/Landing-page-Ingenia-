{
  "buildCommand": "pnpm --filter @workspace/landing-page run build:client",
  "outputDirectory": "artifacts/landing-page/dist/client",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
